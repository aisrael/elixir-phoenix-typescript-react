.dockerignore
Dockcerfile

# there are valid reasons to keep the .git, namely so that you can get the
# current commit hash
.git/
.gitignore

.log
tmp

# Mix artifacts
_build
deps
*.ez
releases

# Generate on crash by the VM
erl_crash.dump

# Static artifacts
node_modules
